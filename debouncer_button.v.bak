module debouncer_button(input button, input clk, output reg button_out);

reg [2:0] state = 3'b0;
reg debouncer = 1'b0;

always @(posedge clk) begin
	case (state)
		3'b0: begin
			if(button) state <= state + 1'b1;
		end
		3'b1: begin
			if(button) state <= state + 1'b1;
			else state <= 3'b0;
		end
		3'b2: begin 
			if(button) begin
				state <= state + 1'b1;
				debouncer <= 1'b1;
			end
			else state <= 3'b0;
			
		end
		3'b3: begin
			if(button) 
			
			
					
end

assign 
endmodule